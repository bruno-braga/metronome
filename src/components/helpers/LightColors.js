const ON_COLOR = 'green'
const OFF_COLOR = 'grey'
const HEAD_COLOR = 'red'
const COLOR_LEVEL = 6

export default {
  ON: `bg-${ON_COLOR}-${COLOR_LEVEL}`,
  OFF: `bg-${OFF_COLOR}-${COLOR_LEVEL}`,
  HEAD: `bg-${HEAD_COLOR}-${COLOR_LEVEL}`
}
